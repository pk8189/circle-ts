export type MockWirePaymentBeneficiaryBankInstruction = {
  /**
   * Virtual account number or Circle corporate Silvergate Wire account number that needs to be set as destination.
   */
  accountNumber: string;
};
